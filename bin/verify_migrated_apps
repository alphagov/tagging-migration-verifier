#!/usr/bin/env ruby

require_relative '../lib/tagging_sync_verifier'

MIGRATED_APPS = %w[
  businesssupportfinder
  calculators
  calendars
  licencefinder
  smartanswers
]

MIGRATED_APPS.each do |app_name|
  TaggingSyncVerifier.new(app_name).verify_taggings_are_in_sync
end
